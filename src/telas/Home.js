import React from "react";
import { View, Text, StyleSheet  } from "react-native";

const Home = ()=>{

    const name = 'HOME';
  
    return(
      <View>
        <Text>{name}</Text>
      </View>
    );
  
}

export default Home;